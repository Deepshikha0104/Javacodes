package Arrayss;

public class WaterTank {
    public static void main(String[] args) {
        int max = 8;
        int tank1[] = {2,8,9,6,7,4,5,7};
        int tank2[] = {1,2,3,4,5,6,7,7};
        Object arr;
                for(int i = 0;i<=arr.length; i++) {
            for(int j =i+1; j < arr.length;j++) {
                int length = min(tank1, tank2);
                int breadth = index(tank1 - tank2);
                curr.area = length * breadth;
                if(curr.area > max) {
                    max = curr.area;
                }
            }
        }

    }
}